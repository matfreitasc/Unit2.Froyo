// This is the main function that will be called by the index.js file

const placeOrders = () => {}

/**
 * This function will take in an object containing the flavors and the quantity of each flavor to be ordered
 * @param {Object.<string, number>} flavors - An object containing the flavors and the quantity of each flavor to be ordered
 * @returns {flavors} - An object containing the flavors and the quantity of each flavor that was ordered
 */

const countFlavors = (flavors) => {
	// Your code here
}
